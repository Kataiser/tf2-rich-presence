import datetime
import os
import time

import psutil
import psutil._exceptions as ps_exceptions
from discoIPC import ipc


def main():
    # generated by 'map list generator.py'
    map_gamemodes = {'ctf_2fort': ('2Fort', 'ctf', 'Capture the Flag'), 'ctf_2fort_invasion': ('2Fort Invasion', 'ctf', 'Capture the Flag'), 'ctf_doublecross': ('Double Cross', 'ctf', 'Capture the Flag'), 'ctf_landfall': ('Landfall', 'ctf', 'Capture the Flag'), 'ctf_sawmill': ('Sawmill', 'ctf', 'Capture the Flag'), 'ctf_turbine': ('Turbine', 'ctf', 'Capture the Flag'), 'ctf_well': ('Well', 'ctf', 'Capture the Flag'), 'cp_5gorge': ('5Gorge', 'control-point', 'Control Point'), 'cp_badlands': ('Badlands', 'control-point', 'Control Point'), 'cp_coldfront': ('Coldfront', 'control-point', 'Control Point'), 'cp_fastlane': ('Fastlane', 'control-point', 'Control Point'), 'cp_foundry': ('Foundry', 'control-point', 'Control Point'), 'cp_freight_final1': ('Freight', 'control-point', 'Control Point'), 'cp_granary': ('Granary', 'control-point', 'Control Point'), 'cp_gullywash_final1': ('Gullywash', 'control-point', 'Control Point'), 'cp_metalworks': ('Metalworks', 'control-point', 'Control Point'), 'cp_powerhouse': ('Powerhouse', 'control-point', 'Control Point'), 'cp_process_final': ('Process', 'control-point', 'Control Point'), 'cp_sunshine_event': ('Sinshine', 'control-point', 'Control Point'), 'cp_snakewater_final1': ('Snakewater', 'control-point', 'Control Point'), 'cp_sunshine': ('Sunshine', 'control-point', 'Control Point'), 'cp_vanguard': ('Vanguard', 'control-point', 'Control Point'), 'cp_well': ('Well', 'control-point', 'Control Point'), 'cp_yukon_final': ('Yukon', 'control-point', 'Control Point'), 'cp_dustbowl': ('Dustbowl', 'attack-defend', 'Attack/Defend'), 'cp_egypt_final': ('Egypt', 'attack-defend', 'Attack/Defend'), 'cp_gorge': ('Gorge', 'attack-defend', 'Attack/Defend'), 'cp_gorge_event': ('Gorge Event', 'attack-defend', 'Attack/Defend'), 'cp_gravelpit': ('Gravel Pit', 'attack-defend', 'Attack/Defend'), 'cp_junction_final': ('Junction', 'attack-defend', 'Attack/Defend'), 'cp_manor_event': ('Mann Manor', 'attack-defend', 'Attack/Defend'), 'cp_mercenarypark': ('Mercenary Park', 'attack-defend', 'Attack/Defend'), 'cp_mossrock': ('Mossrock', 'attack-defend', 'Attack/Defend'), 'cp_mountainlab': ('Mountain Lab', 'attack-defend', 'Attack/Defend'), 'cp_snowplow': ('Snowplow', 'attack-defend', 'Attack/Defend'), 'cp_steel': ('Steel', 'attack-defend', 'Attack/Defend'), 'cp_degrootkeep': ('DeGroot Keep', 'attack-defend', 'Attack/Defend (Medieval Mode)'), 'cp_standin_final': ('Standin', 'control-point', 'Control Point (Domination)'), 'tc_hydro': ('Hydro', 'territorial-control', 'Territorial Control'), 'pl_badwater': ('Badwater Basin', 'payload', 'Payload'), 'pl_barnblitz': ('Barnblitz', 'payload', 'Payload'), 'pl_borneo': ('Borneo', 'payload', 'Payload'), 'pl_fifthcurve_event': ('Brimstone', 'payload', 'Payload'), 'pl_cactuscanyon ': ('Cactus Canyon', 'payload', 'Payload'), 'pl_enclosure_final': ('Enclosure', 'payload', 'Payload'), 'pl_frontier_final': ('Frontier', 'payload', 'Payload'), 'pl_goldrush': ('Gold Rush', 'payload', 'Payload'), 'pl_millstone_event': ('Hellstone', 'payload', 'Payload'), 'pl_hoodoo_final': ('Hoodoo', 'payload', 'Payload'), 'pl_snowycoast': ('Snowycoast', 'payload', 'Payload'), 'pl_swiftwater_final1': ('Swiftwater', 'payload', 'Payload'), 'pl_thundermountain': ('Thunder Mountain', 'payload', 'Payload'), 'pl_upward': ('Upward', 'payload', 'Payload'), 'plr_bananabay': ('Banana Bay', 'payload-race', 'Payload Race'), 'plr_hightower_event': ('Helltower', 'payload-race', 'Payload Race'), 'plr_hightower': ('Hightower', 'payload-race', 'Payload Race'), 'plr_nightfall_final': ('Nightfall', 'payload-race', 'Payload Race'), 'plr_pipeline': ('Pipeline', 'payload-race', 'Payload Race'), 'koth_badlands': ('Badlands', 'koth', 'King of the Hill'), 'koth_brazil': ('Brazil', 'koth', 'King of the Hill'), 'koth_viaduct_event': ('Eyeaduct', 'koth', 'King of the Hill'), 'koth_lakeside_event': ('Ghost Fort', 'koth', 'King of the Hill'), 'koth_harvest_final': ('Harvest', 'koth', 'King of the Hill'), 'koth_harvest_event': ('Harvest Event', 'koth', 'King of the Hill'), 'koth_highpass': ('Highpass', 'koth', 'King of the Hill'), 'koth_king': ('Kong King', 'koth', 'King of the Hill'), 'koth_lakeside_final': ('Lakeside', 'koth', 'King of the Hill'), 'koth_lazarus': ('Lazarus', 'koth', 'King of the Hill'), 'koth_maple_ridge_event': ('Maple Ridge Event', 'koth', 'King of the Hill'), 'koth_moonshine_event': ('Moonshine Event', 'koth', 'King of the Hill'), 'koth_nucleus': ('Nucleus', 'koth', 'King of the Hill'), 'koth_probed': ('Probed', 'koth', 'King of the Hill'), 'koth_sawmill': ('Sawmill', 'koth', 'King of the Hill'), 'koth_suijin': ('Suijin', 'koth', 'King of the Hill'), 'koth_viaduct': ('Viaduct', 'koth', 'King of the Hill'), 'sd_doomsday_event': ('Carnival of Carnage', 'special-delivery', 'Special Delivery'), 'sd_doomsday': ('Doomsday', 'special-delivery', 'Special Delivery'), 'mvm_bigrock': ('Bigrock', 'mvm', 'Mann vs. Machine'), 'mvm_coaltown': ('Coal Town', 'mvm', 'Mann vs. Machine'), 'mvm_decoy': ('Decoy', 'mvm', 'Mann vs. Machine'), 'mvm_example': ('Example', 'mvm', 'Mann vs. Machine'), 'mvm_ghost_town': ('Ghost Town', 'mvm', 'Mann vs. Machine'), 'mvm_mannhattan': ('Mannhattan', 'mvm', 'Mann vs. Machine'), 'mvm_mannworks': ('Mannworks', 'mvm', 'Mann vs. Machine'), 'mvm_rottenburg': ('Rottenburg', 'mvm', 'Mann vs. Machine'), 'rd_asteroid': ('Asteroid', 'beta-map', 'Robot Destruction'), 'ctf_foundry': ('Foundry', 'mannpower', 'Mannpower'), 'ctf_gorge': ('Gorge', 'mannpower', 'Mannpower'), 'ctf_hellfire': ('Hellfire', 'mannpower', 'Mannpower'), 'ctf_thundermountain': ('Thunder Mountain', 'mannpower', 'Mannpower'), 'pass_brickyard': ('Brickyard', 'passtime', 'PASS Time'), 'pass_district': ('District', 'passtime', 'PASS Time'), 'pass_timbertown': ('Timberlodge', 'passtime', 'PASS Time'), 'pd_pit_of_death_event': ('Pit of Death', 'player-destruction', 'Player Destruction'), 'pd_watergate': ('Watergate', 'player-destruction', 'Player Destruction')}
    match_types = {'match group 12v12 Casual Match': 'Casual', 'match group MvM Practice': 'MvM', 'match group 6v6 Ladder Match': 'Competitive'}
    start_time = int(time.time())
    activity = {'details': 'In menus',  # this is what gets modified and sent to Discord via discoIPC
                'timestamps': {'start': start_time},
                'assets': {'small_image': 'tf2_icon_small', 'small_text': 'Team Fortress 2', 'large_image': 'main_menu', 'large_text': 'In menus'},
                'state': ''}
    client_connected = False

    while True:
        tf2_is_running = False
        steam_is_running = False

        # looks through all running processes to determine if tf2 is running and to find the paths of tf2 and steam
        for pid in psutil.pids():
            try:
                p = psutil.Process(pid)

                if p.name() == "hl2.exe" and 'Team Fortress 2' in p.cmdline()[0]:
                    tf2_location = p.cmdline()[0][:-7]
                    tf2_is_running = True

                if p.name() == "Steam.exe":
                    steam_location = p.cmdline()[0][:-9]
                    steam_is_running = True
            except ps_exceptions.NoSuchProcess:
                pass
            except ps_exceptions.AccessDenied:
                pass

        if steam_is_running:
            # reads a steam config file
            steam_config_file(steam_location)

        # used for display only
        current_time = datetime.datetime.now()
        current_time_formatted = current_time.strftime('%I:%M:%S %p')

        if tf2_is_running:
            if not client_connected:
                # connects to Discord
                client = ipc.DiscordIPC('429389143756374017')
                client.connect()

                # sends first status, starts on main menu
                start_time = int(time.time())
                activity['timestamps']['start'] = start_time
                client.update_activity(activity)
                client_connected = True

            # defaults
            current_map = ''
            current_class = ''

            # modifies a few tf2 config files
            class_config_files(tf2_location)

            # console.log is a log of tf2's console (duh), only exists if tf2 has -condebug (see the bottom of config_files)
            consolelog_filename = '{}tf\\console.log'.format(tf2_location)
            with open(consolelog_filename, 'r', errors='replace') as consolelog_file:
                line = consolelog_file.readline()

                # iterates though every line in the log (I KNOW) and learns everything from it
                while line != '':
                    if 'Map: ' in line:
                        current_map = line[5:-1]
                        current_class = 'unselected'  # this variable is poorly named

                    if 'selected' in line and 'candidates' not in line:
                        current_class = line[:-11]

                    if 'Lobby destroyed' in line or 'Steam config directory:' in line or 'Dropped ' in line or 'Disconnect: ' in line:
                        current_map = 'In menus'  # so is this one
                        current_class = 'Not queued'

                    if '[PartyClient] Entering queue for ' in line:
                        current_map = 'In menus'

                        current_class = 'Queued for {}'.format(match_types[line[33:-1]])

                    if '[PartyClient] Leaving queue' in line:
                        current_class = 'Not queued'

                    line = consolelog_file.readline()

            if current_map != 'In menus' and current_map != 'In queue':
                # not in menus = in a game
                try:
                    map_fancy, current_gamemode, gamemode_fancy = map_gamemodes[current_map]
                    activity['details'] = 'Map: {}'.format(map_fancy)
                    activity['assets']['large_image'] = current_gamemode
                    activity['assets']['large_text'] = gamemode_fancy
                except KeyError:
                    activity['details'] = 'Map: {}'.format(current_map)
                    activity['assets']['large_image'] = 'unknown_map'
                    activity['assets']['large_text'] = 'Unknown gamemode'

                activity['state'] = 'Class: {}'.format(current_class)
            else:
                # in menus displays the main menu
                activity['details'] = current_map
                activity['state'] = current_class
                activity['assets']['large_image'] = 'main_menu'
                activity['assets']['large_text'] = 'Main menu'

            # output to terminal, just for monitoring
            print(current_time_formatted)
            print("{} ({})".format(activity['details'], activity['assets']['large_text']))
            print(activity['state'])
            time_elapsed = time.time() - start_time
            print("{:02}:{:02} elapsed".format(int(time_elapsed / 60), round(time_elapsed % 60)))
            print()

            # send everything to discord
            client.update_activity(activity)
        else:
            if client_connected:
                try:
                    client.disconnect()  # doesn't work...
                except:
                    pass

                raise SystemExit  # ...but this does
            else:
                print("{}\nNot running TF2\n".format(current_time_formatted))

            # to prevent connecting when already connected
            client_connected = False

        # rich presence only updates every 15 seconds, but it listens constantly so sending every 5 seconds is fine
        time.sleep(5)


# allows the console to output 'class selected' on class choose
def class_config_files(exe_location):
    tf2_classes = ['Scout', 'Soldier', 'Pyro', 'Demoman', 'Heavy', 'Engineer', 'Medic', 'Sniper', 'Spy']

    for tf2_class in tf2_classes:
        # 'echo' means 'output to console' in source-speak
        selected_line = 'echo "{} selected"'.format(tf2_class)

        config_filename = tf2_class.lower().replace('heavy', 'heavyweapons')  # valve why

        # config files are at 'Steam\steamapps\common\Team Fortress 2\tf\cfg'
        try:
            # reads each existing class.cfg
            class_config_file = open('{}tf\\cfg\\{}.cfg'.format(exe_location, config_filename), 'r+', errors='replace')
            if selected_line not in class_config_file.read():
                # if it doesn't already have the echo line, add it
                class_config_file.write('\n\n' + selected_line)
        except FileNotFoundError:
            # the config file doesn't exist, so create it and add the echo line
            class_config_file = open('{}tf\\cfg\\{}.cfg'.format(exe_location, config_filename), 'w')
            class_config_file.write('\n\n' + selected_line)

        # I know a 'with open()' is better but eh
        class_config_file.close()


# reads steams launch options save file to find -condebug
def steam_config_file(exe_location):
    found_condebug = False

    for user_id_folder in next(os.walk(exe_location + 'userdata'))[1]:  # possibly multiple users for the same steam install
        try:
            # 'C:\Program Files (x86)\Steam\userdata\*user id number*\config\localconfig.vdf'
            with open('{}userdata\\{}\\config\\localconfig.vdf'.format(exe_location, user_id_folder), 'r+', errors='replace') as global_config_file:
                lines = global_config_file.readlines()
                tf2_line_num = 0

                for line in enumerate(lines):
                    if line[1] == '\t\t\t\t\t"440"\n':  # looks for the tf2's number and finds the line number
                        tf2_line_num = line[0]

                for line_offset in range(1, 21):
                    launchoptions_line = lines[tf2_line_num + line_offset]
                    if 'LaunchOptions' in launchoptions_line:
                        if '-condebug' in launchoptions_line:
                            # oh also this might be slow to update
                            found_condebug = True
        except FileNotFoundError:
            pass

    if not found_condebug:
        # yell at the user to fix their settings
        print("\nYour TF2 installation doesn't seem to be set up properly. To fix:"
              "\n1. Right click on Team Fortress 2 in your Steam library"
              "\n2. Open properties (very bottom)"
              "\n3. Click \"Set launch options...\""
              "\n4. Add -condebug"
              "\n5. OK and Close\n")
        # -condebug is kinda necessary so just exit if it's not there
        raise SystemExit


if __name__ == '__main__':
    main()
